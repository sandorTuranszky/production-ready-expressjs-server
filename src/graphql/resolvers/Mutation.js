'use strict';

const Mutation = {
  createUser(parent, args, { prisma }, info) {}, // eslint-disable-line
  deleteUser(parent, args, { prisma }, info) {}, // eslint-disable-line
  updateUser(parent, args, { prisma }, info) {}, // eslint-disable-line
};

module.export = Mutation;
